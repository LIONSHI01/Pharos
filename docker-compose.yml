version: "3"

services:
  node-app:
    container_name: pharos_bot
    build: .
    volumes:
      - .:/usr/src/app
    environment:
      NODE_ENV: development
        - .env
    command: /bin/sh -c "node index.js"
    restart: always
